<?php
/**
 * @var MeetingRoom\Form\PcForm $form
 * @var MeetingRoom\Entity\PC $pc
 */
if(!$this->error){
    ?>
    <div>
        <h3><?php echo 'Редактирование записи с названием '.$pc->getTitle()?></h3>
    </div>
    <?php
    $form = $this->form;
    $pc = $this->pc;
    $url = $this->url('rooms', array('controller' => 'pc', 'action' => 'edit', 'id' => $this->pc->getId()));
    $form->setAttribute('action', $url)->prepare();

    echo $this->partial(
        'meeting-room/form/update-pc',
        array('form' => $form)
    );
}
else{
    ?>
    <div>
        <h3><?php echo $this->error?></h3>
    </div>
<?php
}
